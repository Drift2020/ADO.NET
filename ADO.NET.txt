1. API субд - скорость работы высокая если работать на прямую
2. ODBC - open data base conectiviti , технология, работаем черз прослойку(ПП)

	c помощью драйверов (набор функций для работы с диспечером ODBC)
	работас релиционой бд
3. DAO -> (MS Access)
4. OLE DB (ООП) 
5. ADO ^(провайдер)
6. ADO.NET ^ + .Net


системный виден всем 
пользовательский тоько тому кто создал

для sql server SqlClilent

ольшие файлыне хранять в БД обычно хранят пути к файлам которые на сервере

файл конф - хранит настройки приложения , если нарушенн то приложение не стартанёт, 

oledb-> дольше
sqlclient-> под scl быстрее

async - пока просто декоратор в имени функции 
	нужно вызвать await 
	await - преобразование типов
для каждого метода есть его асинхронный метод